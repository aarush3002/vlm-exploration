# global_frame: odom
# robot_base_frame: base_frame
# update_frequency: 10.0
# publish_frequency: 5.0
# transform_tolerance: 1.0

# # This costmap is a rolling window that moves with the robot
# static_map: true #false
# rolling_window: true
# width: 4.0
# height: 4.0
# resolution: 0.01

# plugins:
#   - {name: obstacle_layer,  type: "costmap_2d::ObstacleLayer"}
#   - {name: inflation_layer, type: "costmap_2d::InflationLayer"}

# # Configuration for the obstacle layer plugin
# obstacle_layer:
#   observation_sources: laser_scan_sensor
#   laser_scan_sensor: {sensor_frame: laser, data_type: LaserScan, topic: scan, marking: true, clearing: true}

# # Configuration for the inflation layer plugin
# inflation_layer:
#   inflation_radius: 0.0

global_frame: odom
robot_base_frame: base_frame

# Use the EXACT same physical radius
robot_radius: 0.1

update_frequency: 5.0
publish_frequency: 2.0
transform_tolerance: 1.5
static_map: false
rolling_window: true
width: 4.0
height: 4.0
resolution: 0.05

plugins:
  - {name: obstacle_layer,  type: "costmap_2d::ObstacleLayer"}
  - {name: inflation_layer, type: "costmap_2d::InflationLayer"}

obstacle_layer:
  observation_sources: laser_scan_sensor
  laser_scan_sensor: {sensor_frame: laser, data_type: LaserScan, topic: scan, marking: true, clearing: true}

inflation_layer:
  # Use the EXACT same safety buffer
  inflation_radius: 0.2 #0.25