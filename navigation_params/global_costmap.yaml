# global_frame: map
# robot_base_frame: base_frame
# update_frequency: 5.0
# publish_frequency: 2.0
# transform_tolerance: 1.0

# # This costmap is based on the static map from SLAM
# static_map: true
# track_unknown_space: true

# plugins:
#   - {name: static_layer,    type: "costmap_2d::StaticLayer"}
#   - {name: obstacle_layer,  type: "costmap_2d::ObstacleLayer"}
#   - {name: inflation_layer, type: "costmap_2d::InflationLayer"}

# # Configuration for the obstacle layer plugin
# obstacle_layer:
#   observation_sources: laser_scan_sensor
#   laser_scan_sensor: {sensor_frame: laser, data_type: LaserScan, topic: scan, marking: true, clearing: true}

# static_layer:
#   subscribe_to_updates: true

# # Configuration for the inflation layer plugin
# inflation_layer:
#   inflation_radius: 0.0 # Your reduced, reasonable value

global_frame: map
robot_base_frame: base_frame

# Define a smaller physical radius for the robot
robot_radius: 0.1

update_frequency: 2.0
publish_frequency: 1.0
transform_tolerance: 1.5
static_map: true
track_unknown_space: true

plugins:
  - {name: static_layer,    type: "costmap_2d::StaticLayer"}
  - {name: obstacle_layer,  type: "costmap_2d::ObstacleLayer"}
  - {name: inflation_layer, type: "costmap_2d::InflationLayer"}

static_layer:
  subscribe_to_updates: true

obstacle_layer:
  observation_sources: laser_scan_sensor
  laser_scan_sensor: {sensor_frame: laser, data_type: LaserScan, topic: scan, marking: true, clearing: true}

inflation_layer:
  # Use a smaller safety buffer
  inflation_radius: 0.2 #0.35